import { Routes } from '@angular/router';
import { AlbumsPageComponent } from './components/albums-page/albums-page';

export const routes: Routes = [{ path: 'albums', component: AlbumsPageComponent }];
